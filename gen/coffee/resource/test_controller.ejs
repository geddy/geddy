assert = require('assert')
controller = geddy.controller.create '<%= names.constructor.plural %>'

tests =
'test stub, replace with your own passing test': () ->
	assert.equal(true, false);


module.exports = tests;
